export type UserItemProps = {
    item : {
        profile_pic_url: string,
        ig_username: string,
        nationality?: string,
        ig_num_followers: number,
        ig_engagement: number,
        hide_new_marker: boolean
    }
}